namespace XcyUI.models
{
    public enum XCursorType
    {
        Arrow,
        Input,
        Crosshair,
        Hand,
        HResize,
        VResize,
        NwseResize,
        NeswResize,
        AllResize,
        NotAllowed
    }
}
